<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:padding="8dp"
        android:background="#222">

        <TextView
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="E-Commerce App"
            android:textColor="#fff"
            android:textStyle="bold"/>

        <ImageButton
            android:id="@+id/searchBtn"
            android:src="@android:drawable/ic_menu_search"
            android:background="@android:color/transparent"/>

        <ImageButton
            android:id="@+id/cartBtn"
            android:src="@android:drawable/ic_menu_add"
            android:background="@android:color/transparent"/>

        <ImageButton
            android:id="@+id/viewToggle"
            android:src="@android:drawable/ic_menu_sort_by_size"
            android:background="@android:color/transparent"/>

    </LinearLayout>

    <SearchView
        android:id="@+id/searchView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <LinearLayout
            android:orientation="horizontal">

            <Button android:text="All"/>
            <Button android:text="Electronics"/>
            <Button android:text="Clothing"/>
            <Button android:text="Books"/>
            <Button android:text="Food"/>
            <Button android:text="Toys"/>

        </LinearLayout>
    </HorizontalScrollView>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

</LinearLayout>